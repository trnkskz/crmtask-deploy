{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview",
    "test": "jest -c ./jest.config.js --runInBand --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify:pricing-fallback": "grep -q 'DEFAULT_PRICING_DATA' src/js/controllers/pricingController.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.10"
  }
}
